'use strict';const{createGovernedRouter}=require('./router');const pool=require('../db');const auth=require('../middleware/auth');const{evaluate}=require('./domain');module.exports=createGovernedRouter({pool,auth,evaluate,workflowType:'kitchen-order-operations',providers:['marketplace','pos','kds','inventory','procurement','delivery','payments','accounting'],approverRoles:['location_manager','food_safety_manager','finance','admin']});
